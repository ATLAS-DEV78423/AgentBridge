# Phase 4: Normalization + Compatibility Engine

**Goal:** Convert scanner output to canonical resources, evaluate compatibility, generate migration plans.

**Current:** 43 tests, 3 scanners, basic compat engine, capabilities registry.

## Tasks

### Task 1: Compatibility Rules
- Create `src/registry/rules.ts` with `getRules(source, target)`
- Rules: claude->opencode (instruction=DIRECT, skill=ADAPTED, hook=UNSUPPORTED), claude->kilo (instruction=DIRECT, skill=DIRECT)
- Test: returns rules, handles unknown, validates fields

### Task 2: Normalizer
- Create `src/core/normalize/normalizer.ts` with `normalizeBundle(bundle)`
- Converts AgentBundle to NormalizedResource[] with capability field
- Test: normalizes bundle, preserves provenance

### Task 3: Enhanced Engine
- Add `evaluateResources(resources, source, target)` to engine.ts
- Uses getRules for batch evaluation
- Test: evaluates multiple resources

### Task 4: Integration Test
- Test scan -> normalize -> compat pipeline end-to-end

### Task 5: Plan Command
- Add `agentbridge plan <source> <target> [path]`
- Shows DIRECT/ADAPTED/UNSUPPORTED per resource
- Test: outputs compatibility report

## Verification
- npm test passes
- agentbridge plan claude-code opencode shows migration analysis
